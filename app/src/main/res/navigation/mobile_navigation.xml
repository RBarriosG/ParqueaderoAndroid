<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_parqueados">

    <fragment
        android:id="@+id/nav_historial"
        android:name="co.com.ceiba.parqueadero.ui.historiales.HistorialesFragment"
        android:label="@string/menu_historial"
        tools:layout="@layout/fragment_historiales" />

    <fragment
        android:id="@+id/nav_parqueados"
        android:name="co.com.ceiba.parqueadero.ui.parqueado.ParqueadoFragment"
        android:label="@string/menu_parqueados"
        tools:layout="@layout/fragment_parqueados" />
</navigation>